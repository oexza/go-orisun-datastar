version: "2"

sql:
  - engine: postgresql
    schema: ./migrations
    queries: ./queries
    gen:
      go:
        package: dbsql
        out: internal/dbsql
        sql_package: pgx/v5
        emit_json_tags: true
        emit_pointers_for_null_types: true
